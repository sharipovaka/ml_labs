node_modules/
dist/
.cache/

# Служебные файлы ОС и редакторов
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
npm-debug.log*

# Чекпойнты Jupyter
.ipynb_checkpoints/

# Не публикуем на GitHub: техническое описание проекта и служебные материалы.
# Файлы остаются локально; на сайт это не влияет — весь публикуемый контент
# уже собран в src/content/.
README.md
тех от бездушной машины/
lectures-tex/

# Промежуточные файлы LaTeX
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
_build_lec*.tex

# ВАЖНО: сгенерированный из ноутбуков HTML НЕ игнорируется —
# он импортируется в бандл, поэтому должен быть в репозитории.
# (Если вы хотите генерировать его только в CI, раскомментируйте строку ниже
#  и добавьте шаг `npm run convert` в GitHub Actions.)
# src/content/**/*.html
